# factor_things

To run this you have to do something like:

```
git clone ...
cd ...
mkvirtualenv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

And in a second tab:

```
mongod
```
